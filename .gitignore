# dependencias
node_modules/

# secretos / config local (nunca subir)
.env
.env.*
*.key
.chalc.json
.rig.json

# trazas de IA por spec (specs/<id>/.chalc/ai-trace.jsonl) — no subir
.chalc/

# sistema
.DS_Store

# sandbox de pruebas (no subir)
prueba/

# logs / temporales
*.log
tmp/

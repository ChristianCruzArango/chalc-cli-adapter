{
  "id": "angular-cli",
  "kind": "npx",
  "description": "Servidor MCP oficial del Angular CLI (on-demand, no requiere instalar nada).",
  "requiresSecret": false,
  "server": {
    "command": "npx",
    "args": ["-y", "@angular/cli", "mcp"]
  }
}
